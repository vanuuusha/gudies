Докер - все бинарники, код, библиотеки, но не содержит операционную систему и ядрно
Гипервизор - несколько операционных систем параллельно с основной
docker image - файл с инструкциями, которые будут выполнены когда мы запустим контенер
docker container - запущенный инстанс докер image
dockerfile - файл с командыми, выполняя которые собирается докер image
docker engine - это программа-хост для контенеров
