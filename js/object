var a = {
	
	drive: function () {
	
	}
} - через двоеточие и запятую, после ласт свойства запятая не ставиться
value может быть функцией

for (var prop in chevy) - prop присваиваются по одному названия свойства

обращение либо через точку либо в квадратных скобках

instanseof - является ли экзепляром объекта
