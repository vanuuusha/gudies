let promise = new Promise(function(resolve, reject) {

} )

resole - функция, которая вызовется при успешном выполнении (встроенная)
reject - функция, которая вызовется при неудачном выполнении (встроенная)

promise.then(функция что делать при успешном выполнение, при неудачном)
promise.catch() - только второй аргумент у then ( promise(null, func) )
promise.finally() = promise.then(f, f) (но без аргументов, также прокидывает ошибки через себя)
