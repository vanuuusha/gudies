Общего назначения:
	ax (accumulator) 
	bx (base)
	cx (count)
	dx (data)
	
Регистровые указателы:
	SP (stack pointer) - указывает на вершину стека
	BP (Base pointer) - адресует переменные, хранимые в стеке
	(адрес фрейма стека) - указывает на начало стека
	
Индексные регистры:
	SI (Source index) - является индексом источника
	DI (Destination index) - является индексом назначения
		
Сегментные регистры: 
	Cs (code segment) - начальный адрес сегмента кода
	DS (data segment) - начальный адрес сегмента данных
	SS (stack segment) - начальный адрес сегмента стека
	ES (Extra segment) - вспомогательный сегментный регистр
	
Ip - указатель на команду 
