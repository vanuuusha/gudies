movs - переслать строку. пересылает из ds:si в es:di
cmps - сравнить строки. ds:si es:di. То что в приемнике сравнивается с тем что в источник(путем вычитания, флаги изменяются)
scas - сравнить строку с регистром. сравнить слово по адрес es:di с ax.
Lods - загрузить строки. загружает в регистр al для байта и в регистр ax для слова из ds:si 
stos - сохранить строку. сохраняет al(ax) по адресу es:di
источник - ds
приемник - es

rep - префикс заставляет повторятся столько раз, сколько указано в cx.
repe=repz
repne=repnz

