adc приемник,источник - число1+число2+cf
sbb приемник,источник - число1-число2-cf

mul множитель(второй) - imul(со знаком)
(mul: al - res: ax)
(mul: ax - res: dx(high):ax(low))

div (делитель) - idiv(со знаком)
(делимое: Ax, частное - al, остаток - ah)
(делимое: Dx:ax, частное - Ax, остаток - dx)

NET - изменяет знак
movsx (слово),(байт) - копирует со знаковым расширением

loop metka - цикл, пока cx не станет равным нулю
loopz metks - дополнительно проверяет ZF=1
loope metka - дополнительно проверяет ZF=0

inc (регистр) - увеличивает на 1 

cbw - расширяет знаковый бит Al на Ah, получаем ax=al
cwd - расширяет знаковый бит ax на все биты dx

in - чтение данных из порта ввода вывода in al,60h (приемник, номер порта)
out - вывод в порт ввода-вывода (младший байт, в порт указанный первым операндом, а старший в порт на единичку больше)

LDS - загрузить в указанный регистр и ds из памяти (ds считывается вторым словом)
LES - загрузить в указанный регистр и es из памяти (les ax,[8])
LEA - загрузить эффективный адрес (lea ax,[bx+si])
push - сохранить что-либо в стек
pop - достать из стека
pushf - сохпанить регистр флагов в стек
popf - считать регистр флагов
LAHF - младший байт регистра флагов в AH
SAHF - сохранить регистр ah в младший байт флагов
XLAT - перекодировка по таблицы (bx - начало таблицы, al - байтовое значение, которое нужно перекодировать) а al загрузить bx+al элемент таблички

add - add word ptr [di], 7
adc
sub
sbb
Оба операнда в памяти
inc - не меняет флаг переноса. может работать с памятью 
dec - не меняет флаг переноса
div r/m - как беззнаковые деление (результат - ax(al), остаток - dx(ah)). значение всех флагов не определно
idiv - идентична div, ток со знаком
mul r/m - умножает al(ax) на явный операнд (ax(dx:ax)) если результат не уместился бы в al(ax), то CF и OF
imul - аналогична
neg - если результат не ноль, то CF=1. Если максимально отр значение, то значение в операнде не изменится, но  CF и OF в 1
cbw - не менят флаги
cwd
cmp ax,dx - флаги также как в вычетание, приемника нет

AND - ZF,PF,SF - в соотв с результатом, AF - ?, CF.OF = 0 
OR 
XOR
TEST - как AND, только никуда не пишет результат
NOT - не меняет флаги


