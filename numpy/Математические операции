+, -, *, /, %, ** - массивы должны быть одинакового размера, для двумерных такое умножения остается поэлиментным. Могут автоматический немного преобразовыватся для осуществления операций(можно использовать np.newaxis для контроля этого преобразования)

Поддерживает сравнение как с одиночным элементом, так и с другим массивом
Комбинирование с помощью np.logical_and(,);np.logical_or(,);np.logical_not(,)

в numpy включены стандартные мат функции.

np.floor(a), ceil, rint(ближайшие) - округления

np.pi, np.e - констант

a.sum() - сумма массива
a.prod() - перемножения массива
a.mean() - среднее
a.min(), a.max() 
Могут принимать axis (по какой оси делать)

sorted, sort работают

a.clip(0, 5) - оставляет только элементы в диапазоне от 0 до 5

np.unique(a) - извлекает уникальные элементы

a.diagonal() - возвращает диагональ
