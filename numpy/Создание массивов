np.array([], float) - массив типа float (может содержать только один тип)
np.arange(5, float) - делает тоже что и range только возвращает np массив

np.zeros((2,3), dtype=float) - создает массив нулей
np.ones((2,3), dtype=float) - создает массив единиц

np.zeros_like(a) - заполняяет уже созданный массив нулями
np.ones_like(a) - заполняяет уже созданный массив единицами

a[0,0] - нулевой элемент в главном массиве, 0 во втором (работает аналогично второй скобке)
a[1,:] - первый из главного и все из второго

a.shape - количество строк и столбцов
a.dtype - какой тип переменных хранится в массиве

0 in a - проверяет все элементы в матрице

a = a.reshape((размерность, размерность)) - переформировать массив

a.tolist() - перевести в стандартный список питон

a = a.tostring() - в байт строку
np.fromstring(s) - из байт строки

a.fill(0) - заполняет массив нулями

a.transpose() - возвращает транспонированный массив

a.flatten() - конвертирует массив в одномерный

np.concatenate((a, b, c)) - объединяет (конкатинирует) массивы
np.concatenate((a, b, c), axis=<int>) - axis показывает по какому измерению соединить

a[:, np.newaxis]

