процесс - запущенная программа 
имеют большую безопастность, однако более дорогостоящие
внутри python программы нельзя выполнять более чем один поток за раз, но процессов много
потоки(тред) - делят одно адресное пространство (создается процессом)

каждая программа(синхронная и ассинхронная) может работать в однопоточном и многопоточном режиме
Синхронная модель - каждая задача выполняется полностью (нельзя остановить выполнение задачи) (в многопоточном случае также n потока выполняют n задачи полностью)

Асинхронная модель - задача выполняется по чуть-чуть. Многопоточная асинхронность - когда задачи выполняется многими потоками по чуть-чуть
асинхронная модель дает преимущество, так как задача пока ждет ответ (допустим от сервера) может прекратить свое выполнение


Параллелизм - способ обработки множественных запросов одновременно ()
