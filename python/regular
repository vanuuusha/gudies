. - один произвольный символ
[] - выбор символа из этих
$ - окончание строки
| - логическое ИЛИ
() - группировка символов
{} - количество повторий (можно от и до)

\w - любая цифра, буква, нижнее подчеркивание
\d - любая цифра
\s - любой пробельный символ

re.match - совпадения в начале строки
re.search() - поиск по всей строке
re.findall() - поиск по всем сиволам
re.split() - разделяет строку по выбранному шаблону
re.sub(,на что меняем,) - заменяет по шаблону
re.compile - объект шаблона
answer.group(0) - получить найденное значение
