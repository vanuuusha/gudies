CAST(column AS FLOAT) - меняет тип данных (в данном случае на Float)

Можно делать FROM и подзапрос (и вообще откуда угодно)

group_concat(field) - объединяет одно поле в группе в одно через разделитель

substr_count(строка, подстрока) - сколько раз подстрока встречалась в строке
