ssh -V - версия ssh
ssh-keygen -t rsa(алгоритм) -b 2048 -C "Machine" (комментарий) -f название
ssh -i id_rsa(название ключа) vanusha(пользователь)@130.193.49.58(ипишник)

mkdir -p .ssh - создает папку ssh для пользователя
chmod 700 .ssh - изменяет доступ к папке(без этого папка работать не будет)
/.ssh/authorized_keys - сюда можно добавлять ключи

sudo nano /etc/ssh/sshd_config - тут можно отключить возможность захода по паролю
sudo systemctl reload sshd.service - перепрочтение данного файла

scp -i id_rsa (что скопировать) (имя пользователя)@ип:(путь до папки) - скопировать на машину (если наооборот, то с машины). Ключ -r рекурсивно папку
